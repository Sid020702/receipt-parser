{
  "name": "receipt-parser",
  "private": true,
  "scripts": {
    "dev": "concurrently \"bun run --cwd server dev\" \"bun run --cwd client dev\"",
    "build": "bun run --cwd client build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
